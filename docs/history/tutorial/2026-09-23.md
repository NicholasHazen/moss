# Tutorial checks — September 23, 2026

[Guide verification](../../tutorial/authoring/verification.md) and
[path verification](../../tutorial/path/verification.md) keep current coverage.
These records distinguish documentation checks from installed rules.

## Choice before the meal

Added `docs/tutorial/path/visuals/choice-before-meal.svg` beside session 04's
existing state diagram. The new figure follows its existing two-tick example,
separating the choice inputs (73, then 76) from completed-tick reserves (77, then
76). Both ticks begin at the same patch; upkeep is 1, the bite limit is 4 and
capacity is 100. With no growth or other eater, the first meal leaves 4 biomass;
the next choice clears the target before eating and leaves that remainder alone.

The guide explains why a later meal does not rerun choice. The illustration labels
the scenario as future, omits movement/tick completion for focus, and states that
same-cell movement changes no values. Its title, description and Markdown alt
text carry the trace without relying on color. A bounded read-only review found
the arithmetic and order consistent with the proposed transition and meal rules;
the trace does not establish their live integration.

Served only the visual directory through a temporary loopback-only Python HTTP
server. The initial sandboxed bind was denied; the approved retry bound an
automatically selected local port. In a background in-app Chromium tab, inspected
the SVG screenshot: both rows, arrows, numerical labels, inspector summaries and
footer were readable and unclipped. This is a standalone diagram rendering check;
the lesson's RustRover Markdown preview was not exercised.

No Rust example, live source, schedule or next assignment changed. Native tests,
WASM builds and the Moss browser application were not rerun for this visual-only
change. Today's movement helper remains unfinished and unscheduled.

Validation passed 766 local destinations across 77 Markdown files, SVG XML and
title/description checks, and `git diff --check`. All 29 snapshotted source,
shared configuration, NOW and v2 files were unchanged. Session 04's executable
reference matched its pre-change version. Closed the temporary browser tab and
stopped the loopback server after inspection.

## A shorter way into the current lesson

A bounded read-only review found the root README and NOW already identified the
current edit clearly. Tutorial home introduced the future path twice and repeated
the entire optional context shelf. Removed that duplication and ordered its table
with today's v2 lesson first, followed by the optional meal, future path and
completed maintenance reference. The context section now leads to the existing
organized shelf instead of repeating its seven entries.

Preserved every heading, companion-chapter link and the current lesson's opening
link. No page was moved or removed; the future sixteen-session path retains its
scope, and the context pages remain available. NOW, lesson bodies, Rust examples,
source and run configurations were not edited. This applies TutorBro's guidance
to reduce material that competes with the reader's next action, without claiming
a measured improvement in learning. No native, WASM, browser or IDE check ran.

The final independent read-through found no lost route or ownership information.
All six tutorial-home headings and named companion destinations were preserved.
Validation passed 759 local destinations across 77 Markdown files and
`git diff --check`; all 29 protected source/configuration/NOW/v2 files matched
their recorded hashes. The removed links remain available from the context shelf.

## Wall time and executed ticks

Reviewed the current tick walkthrough and future daylight lesson against
`playback.rs`, `browser/frame.rs` and the installed simulation. The old statement
that a fast monitor does not make an animal hungrier needed a precise boundary:
equal executed ticks and accepted inputs, starting from the same state and rules,
give the same biological result. Equal wall time can contain different executed
tick counts because automatic playback discards excess elapsed time under load.

Added a two-column trace to the context page: after a baseline call at 0 ms,
regular updates through 2,000 ms yield eight ticks and default reserve 52; one
late update at 2,000 ms yields four ticks and reserve 56 with `slowed = true`.
In either case, the next call at 2,250 ms requests one tick. The four-tick cap
belongs to automatic playback; an accepted manual Step requests one full tick.
Session 11 now connects its nominal one-minute day to this timing distinction.

Reused the isolated workspace
`/private/var/folders/dc/n3zh722n3_s3tfb4806dh9mh0000gn/T/moss-path-examples-1jelcra_`
after comparing every copied live Rust file and all three pinned workspace
manifests byte-for-byte with the current project. Added only a scratch integration
target `timing_explanation.rs`. It includes the copied real playback module,
creates a fresh installed simulation per case, feeds explicit timestamps, runs
the returned tick count, and asserts both animals' reserves, completed ticks,
slowdown flags and the following one-tick request.

Commands run in that isolated copy:

```sh
scripts/with-toolchain.sh rustfmt --edition 2024 crates/moss-sim/tests/timing_explanation.rs
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test timing_explanation wall_time_and_executed_ticks_are_different_measures -- --exact
scripts/with-toolchain.sh cargo clippy -p moss-sim --locked --offline --test timing_explanation -- -D warnings
```

The named probe passed; the three included playback tests were filtered out.
Clippy passed. This verifies native arithmetic and installed maintenance for
supplied timestamps, not browser event delivery, a real render stall, WASM or
the future daylight integration. No live Rust or guide reference body changed.

Final independent review confirmed the timing trace and prompted a clarification
in session 11: the 240-tick day is model configuration; playback determines its
viewing rate. Navigation passed 763 local destinations across 77 Markdown files,
with a clean whitespace check. All 29 protected source/configuration/NOW/v2 files
and both edited pages' code fences were unchanged.

## Inspecting overlapping individuals

Reviewed session 08's controlled comparison against the preceding selection,
activity and individual-cost lessons. A bounded independent review confirmed
that its three one-cell ticks, equal starts and unchanged pre-growth biomass
isolate the expected maintenance difference: reserves 51 and 48 after equal
travel. No arithmetic or worked-reference change was needed.

The browser instruction had omitted a useful existing control: both animals
occupy the same cell, so clicking their overlapping markers selects the lower
stable ID. Source inspection of `browser/frame.rs`, `shell.js` and `index.html`
confirmed that separate `Hare #ID` buttons under “In this chamber” select each
animal directly. Reset and Step leave playback paused; selection updates the
view without executing a tick while paused. Revised the closing instruction to
use those buttons at completed tick three, then inspect Meadow's biomass.
A second independent source review confirmed that route.

This is a documentation/source check, not a new browser observation. The future
comparison scenario remains unimplemented. No native tests, WASM build, runtime
UI or RustRover preview were run. The lesson's Rust fences and headings, live
source, shared configuration, NOW and today's v2 exercise remain unchanged.

Validation passed 764 local destinations across 77 Markdown files and
`git diff --check`. All 34 protected source/configuration/current-task files
matched their recorded hashes; session 08's code fences and headings also matched.

## Defaults and owned values illustration

Added an optional interactive conversation illustration at
`/Users/nick/.codex/visualizations/2026/09/20/01a0c105-6b3b-73b1-8900-b14177405987/defaults-and-owned-values.html`.
It follows the proposed individual-cost policy without connecting to Moss:
three authored overrides (`None`, `Some(1)`, `Some(0)`) construct separate owned
rates. Next-run configuration, the active template and an example runtime edit
are distinct state. A bounded independent review confirmed the transitions and
the need to label the runtime edit rather than infer its origin from a number.

The fragment's JavaScript passed `node --check`. The skill's `render.py --serve`
wrapper ran on an automatically selected loopback port after the initial sandbox
bind was denied. In background Chromium, observed template 1 producing 1/1/0;
draft 9 leaving those values unchanged; an instance edit producing 4/1/0; Reset
restoring 1/1/0 while retaining draft 9; and starting that draft producing 9/1/0.
Starting template 0 produced 0/1/0, preserving both explicit overrides. Reload
restored the active configuration, pending draft and edited component value.

Inspected the default layout and a 320-pixel viewport in the current dark theme:
controls, table labels and runtime-edit state were readable and unclipped. An
initial style made a hidden edit label visible; corrected it and repeated the
state checks. The final browser error log was empty. Reset the viewport, closed
the temporary tab and stopped the preview server.

This verifies the illustration, not live ECS ownership, scenario reconstruction,
browser configuration or provenance collection. No Rust source, lesson body,
WASM build, native test or RustRover preview changed or ran. Today's movement
helper remains unfinished and unscheduled.

Documentation validation passed 764 local destinations across 77 Markdown files
and `git diff --check`; all 34 protected source/configuration/current-task files
matched their earlier recorded hashes.

## The supplied movement rate matters

A bounded independent readiness review confirmed a concrete gap in the prepared
test: every call used rate 2. An implementation that ignored `units_per_cell`
and always charged 2 could pass the helper checkpoint and default journey.
Added two cases to the same named test: rate 3 with reserve 3 moves one cell and
leaves zero; rate 3 with reserve 2 stays put and leaves two. Both check returned
distance, position and reserve. All existing assertions remain. V2 and the
movement companion now mention this coverage; neither printed helper changed.

Ran from the live root:

```sh
scripts/with-toolchain.sh cargo fmt --all -- --check
scripts/with-toolchain.sh cargo test -p moss-sim --locked --test movement movement_charges_only_an_affordable_actual_step -- --exact
python3 docs/tutorial/authoring/check_path_examples.py --movement --keep-workspace
```

Formatting passed. The live command compiled and reached exactly one expected
unfinished-helper panic, with two tests filtered out (exit 101). The reference
runner used its own copied workspace and target directory at
`/private/var/folders/dc/n3zh722n3_s3tfb4806dh9mh0000gn/T/moss-path-examples-41yt84wu`.
It confirmed that both printed helpers agree after pinned formatting, passed the
stronger helper test, then activated only that copy and passed all five
movement/maintenance checks with the ignored integration cases explicitly run.
Clippy passed for those targets with warnings denied.

In that isolated copy, temporarily replaced `.checked_mul(units_per_cell)` with
`.checked_mul(2)` in the worked helper. Ran its exact named test against the
saved pre-edit test file and then the strengthened file:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test movement movement_charges_only_an_affordable_actual_step -- --exact
```

The faulty answer passed the previous test. It compiled and failed the new
reserve assertion: actual 1, expected 0. Restored the correct helper and updated
test in a `finally` block, then passed the exact named test again. This proves
the added case detects that specific defect, not general mutation coverage.

The live helper, schedule, activation ignores, configurations and dependencies
are unchanged. This is a test-plumbing refinement, not a new learner assignment
or implemented movement. No full workspace suite, WASM build, browser or native
IDE check was rerun. Browser journey acceptance still follows Nick's helper review.

Final independent diff review found no further issue. Navigation passed 766 local
destinations across 77 Markdown files and `git diff --check` passed. The test diff
contains one insertion and preserves every prior assertion. Of 33 protected
files, only that test and its two lesson coverage descriptions changed; the
other 30 matched their recorded hashes.
