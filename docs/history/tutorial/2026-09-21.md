# Tutorial verification — September 21, 2026

Historical example checks, not current runtime claims.

## Chapter 1D worked refactor, September 21, 2026

Validated both exact Rust fences in `01d-energy-profiles.md` against `86f9f2e`
plus Nick's uncommitted Chapter 1C implementation. Applied the examples only in
`/private/tmp/moss-chapter1d-g5_fdkds`, with its own `CARGO_TARGET_DIR` set to
`/private/tmp/moss-chapter1d-g5_fdkds/target`. No live Rust source was changed.

Through `scripts/with-toolchain.sh`, ran `cargo fmt --all`,
`cargo test -p moss-sim --locked --offline`, and `cargo fmt --all -- --check`.
Observed **seven passing tests** (one unit, six integration), zero failures,
and clean formatting. All 59 original bootstrap assertion statements were
retained unchanged, including the default-rate and custom-rate expectations.
The unchanged lookup lets `spend_energy` keep its existing implementation.

Static validation passed for 197 local guide/plan links and fenced-block closure
across 18 Markdown files. Chapter 1D remains a proposed learner edit in the live
project; reset verification, inspector plumbing and browser acceptance remain
pending. No WASM or browser check was run for this isolated example.

## Live Chapter 1C green checkpoint, September 21, 2026

Reviewed `86f9f2e` plus Nick's uncommitted species settings, lookup, rate-aware
`spend_energy` and custom-rate regression. The resource initialization and
original default-rate regression remain in place. The loop matches Chapter 1C:
it reads the shared rates, queries species alongside mutable energy, and
saturates at zero. No later biological rule was added.

Ran `scripts/with-toolchain.sh cargo test -p moss-sim --locked`: **seven tests
passed**, one unit and six integration, with zero failures. The suite exercises
the installed schedule with both default and custom rates. The custom test
observes Hare 57 / Fox 54 after three ticks and both animals present at zero.
`cargo fmt --all -- --check` initially reported only a missing trailing comma
in the system parameter list; formatting was applied afterward and the check
then passed. `git diff --check` passed, as did a static check of 180 local guide
and plan links plus fenced-block closure across 17 Markdown files.

Updated current guide status and explanations while preserving chapter anchors
and the A–C worked examples. The smaller borrowing example is explicitly the
preceding version of the function. Source comment cleanup changes no behavior.

**Still pending for Chapter 1 acceptance:** live custom-rate reset verification,
inspector rate display, browser observations with default and custom settings,
and broader native/WASM checks. The native test run does not establish these.
The RustRover diagnostics investigation is separate; these source checks found
no compilation error in the simulation code.

## Live Chapter 1 checkpoint, September 21, 2026

This records the earlier red checkpoint B, before the green result above.

Reviewed `86f9f2e` plus Nick's uncommitted resource, lookup and custom-rate test.
The focused test initially failed to compile with E0422 because `SpecialEnergyRules`
and `SpeciesEnergyRules` were mixed. Corrected the name/import, initialized the
resource in `install`, and restored the original regression beside the new one.
The maintenance loop remains Nick's next edit.

Ran `scripts/with-toolchain.sh cargo fmt --all` and
`scripts/with-toolchain.sh cargo test -p moss-sim --locked`.
Observed six existing tests pass (one unit, five integration) and the new
integration test fail with Fox actual 57 versus expected 54. This is the intended
red checkpoint B, not a green suite. No broader checks or WASM/browser smoke
test were run for this draft. Resume at checkpoint C.

The guide targets this repository's pinned Rust 1.93.1 and Bevy ECS 0.18.1.
Worked code is checked in an isolated copy; it is not installed into the live
simulation by writing this guide. The validation record below distinguishes
tested examples from the future integration/browser acceptance checks.

<!-- tutorial-validation -->
## Worked examples, September 21, 2026

The original tested guide was saved in `ee514e9`; live implementation was the
foundation and maintenance rule, with IDE setup from `7c8a22a`. Source anchors
are [lib.rs](../../../crates/moss-sim/src/lib.rs),
[lessons.rs](../../../crates/moss-sim/src/lessons.rs), and
[bootstrap.rs](../../../crates/moss-sim/tests/bootstrap.rs).
This is a historical record of that validation, not an assertion that future
edits have been retested automatically.

Observed:

- Chapter 1A's resource/default example compiles with the existing six simulation tests passing.
- Chapter 1B's new regression fails for the intended reason: Fox actual 57 versus expected 54. Chapter 1C's worked system makes it pass; all seven simulation tests then pass.
- Each helper test in Chapters 3–5 fails against its unfinished stub and passes against the exact worked function. The food-choice test also checks that a closer patch beats a lower-ID, farther patch.
- A separate temporary regression confirms reset preserves custom species rates. With all examples and that check installed in the temporary copy, 11 native simulation tests pass. Formatting and simulation Clippy with warnings denied also pass.
- The live repository's six simulation tests still pass. No live Rust source, dependency or biological behavior was changed for this guide.

Commands used in the temporary copy: the focused `cargo test` commands printed
in the chapters, `cargo test -p moss-sim --locked`, `cargo fmt --all`, and
`cargo clippy -p moss-sim --all-targets --locked -- -D warnings`, through the
repository's toolchain wrapper. Two read-only subagent reviews checked API
accuracy, test coverage and the size of the learning steps.

**Not yet implemented or verified:** population scaffolding, the installed
choice/movement/eating systems, their future integration tests, and the browser
acceptance checks described in these chapters. The helper examples do not claim
those later behaviors work. No new WASM/browser smoke test was run for this
documentation-only change; the current foundation's evidence remains in
[build notes](../../development/verification.md).

## TutorBro revision, September 21, 2026

**Reviewed base:** `ee514e9`, plus this documentation revision. No source or
dependency changes were present. Read the actual TutorBro `SKILL.md`, prose
guide and exemplar reference through Nick's shared skill page in the signed-in
browser. The authoring contract records the source and selected techniques.

Preserved all existing Rust and shell fences in the five chapters byte-for-byte
against `ee514e9`. Their earlier executable validation therefore remains the
relevant record; no future behavior was added to the live simulation. The existing
focused maintenance command was run again while preparing the context pages:
one test passed.

Checked all guide and entry-point local file links and heading anchors, fenced
block closure, and patch whitespace. Two bounded helpers prepared authoring and
context pages; a subsequent understanding/prose review corrected the distinction
between simulation reset, browser camera reset, and future cross-run history.

**RustRover observation:** opened guide home and Chapter 1 directly in the
installed EAP. Both render in Editor and Preview mode with headings, linked
contents, paragraphs and highlighted Rust code. The preview's automatic click
attempt did not navigate, so end-to-end clicking of every link is not claimed.
The link destinations and anchors were checked statically; direct file opening
worked. No extra Markdown plugin or global appearance setting was installed.

**Unchanged limits:** population/foraging integration and future browser behavior
remain acceptance criteria. No simulation WASM/browser smoke test was repeated
for this prose and navigation revision.

## Revised Chapter 1D-A component checkpoint, September 21, 2026

This validation supersedes the earlier Chapter 1D shared-profile-only worked
example for the current next edit. The former result remains historical evidence;
it does not validate the newly planned individual-cost runtime behavior.

Reset the agent-owned scratch workspace at `/private/tmp/moss-chapter1d-g5_fdkds`
from the current live manifests, lockfile, scripts and crate sources. Inserted
the exact 13-line `individual-energy-component` Rust fence from Chapter 1D into
the scratch `lib.rs`, between `Energy` and the existing `SpeciesEnergyRules`.
The existing resource, fixture, maintenance system and tests were unchanged.
The scratch workspace used its own `target` directory, separate from the live
project's build output.

Ran in that isolated workspace:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline
scripts/with-toolchain.sh cargo fmt --all -- --check
```

Observed **seven passing simulation tests** (one unit, six integration), zero
failures, and a clean formatting check. Both commands exited successfully. The
shell emitted an unrelated `pyenv` rehash warning about its non-writable shims;
the Rust compiler, tests and formatting check still completed successfully.

This establishes that the exact checkpoint A type/default declaration compiles
alongside the existing working rule. It does not attach costs to animals or
change the system's species-resource lookup. Individual override resolution,
the same-species regression, default/reset semantics and the future direct
component query remain unimplemented preparation/paired work. The revised
chapter and attributes context label those steps as proposed or future.
No live implementation, WASM/browser smoke test, or performance benchmark was
performed for this checkpoint.

## Applied attribute research, September 21, 2026

Added a [research note](../../research/attributes-and-energy.md) and links from
Chapter 1D and its attributes context. Read official engine documentation,
commit-pinned Veloren source through a bounded research helper, and Factorio
developer reports. The proposed component and all tutorial code fences remain
unchanged. Research examples are explanatory, not executed Moss behavior.
No new native tests, WASM build, browser check or performance benchmark was run.
Statically checked 221 local links and code-fence closure across 22 Markdown
files; all passed. A second source review corrected Flecs inheritance terminology.
No new RustRover rendering or external-link traversal check was performed.

## General property-system research, September 21, 2026

Linked [broader research](../../research/property-bags-and-defaults.md) from the
attributes context after Nick widened the question beyond games. Reviewed WPF,
CSS, OpenUSD 26.08, Figment 0.10.19 and Typesafe Config documentation with two
bounded research helpers. No tutorial code fence, next edit or runtime API changed.
No new RustRover rendering, native/WASM/browser execution or benchmark was run.
Checked 235 local links and code-fence closure across 23 Markdown files and ran
`git diff --check`; all passed. A source review clarified that caches of immutable
snapshots do not need invalidation while their inputs stay unchanged.
