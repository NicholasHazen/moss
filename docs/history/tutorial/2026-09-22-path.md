# Short-session reference verification — September 22, 2026

[Current evidence summary](../../tutorial/path/verification.md) · [Work history](../README.md)

These dated records retain the commands, observations and limits of each
checkpoint. Counts and references to “current” or “today” describe that entry's
stage; later entries can supersede earlier ones. Temporary workspaces are
disposable evidence, not required development checkouts or retained test tools.
Moving this record did not rerun its runtime checks.

**Reviewed and executed September 22, 2026.** These are future learning guides
relative to the unchanged live movement checkpoint. Passing their worked
references does not install those behaviors or establish browser acceptance.
The base is `86f9f2e` plus the existing uncommitted source/document reorganization
and movement-session preparation. No live Rust, manifests, tests or IDE actions
were changed to author this path.

## History coverage checkpoint, September 22

Refined sessions 14–15 and the observability plan after a bounded read-only review.
They now distinguish the first retained event from complete coverage of its tick,
and recorded zero from evicted details or uncollected outcomes. Separate event IDs
are explicitly future work. No new exercise, biological rule or history API was
implemented; all sixteen runnable references remain unchanged.

The retained isolated workspace has `tests/journal_coverage.rs`. It compiles the
unchanged `journal.rs` as a local module to exercise the crate-visible recorder
without expanding the production API. Two new checks establish:

- Thirty-five synthetic records on tick 120 retain 32 and evict three; the oldest
  retained tick remains 120, while the first three participants are absent.
- Three installed maintenance ticks leave two animals at reserve 57 while the
  public journal still contains four tick-0 entries and reports zero evictions.

All simulation source files in that copy were byte-compared with live source
before running. With `CARGO_TARGET_DIR` set to its own `target`, these passed:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test journal_coverage
scripts/with-toolchain.sh cargo clippy -p moss-sim --locked --offline --test journal_coverage -- -D warnings
```

**Observed:** three tests passed—the two new examples plus the existing journal
unit test included with its source. Clippy passed. The synthetic records use the
current initialization event type solely to exercise retention; no death event,
counter or future lifecycle system is present. The current browser's coverage
text already identifies its uncollected outcomes; it was read, not newly exercised.
No WASM build, browser smoke or native RustRover preview was rerun.

## Shared outcomes checkpoint, September 22

A bounded review identified a missing teaching case in session 02: both requests
can qualify before ordering, while only one eventually receives food. Added an
initial-biomass-4 case to the existing reference test. It expects reserves 4 and
0, empty biomass, and exactly one actual `Meal`. The learner's live edit remains
the reversed-order check; no additional required session was added.

The prose and a new diagram contrast current-patch transfers with awards
calculated from a copied observation. Session 03 links target choice back to
that distinction. Rust's official [Copy reference](https://doc.rust-lang.org/std/marker/trait.Copy.html)
and Bevy ECS 0.18.1's [query reference](https://docs.rs/bevy_ecs/0.18.1/bevy_ecs/system/struct.Query.html)
were consulted; actual compilation and regression failures below verify the
specific counterexamples. No memory-safety or parallel-performance claim is
inferred from a browser picture.

Extracted the revised session-02 fence into the retained isolated workspace
`/private/var/folders/dc/n3zh722n3_s3tfb4806dh9mh0000gn/T/moss-path-examples-1o4zl3h_`.
With `CARGO_TARGET_DIR` set to that copy's own `target`, these passed:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test month_guide session_02:: -- --nocapture
scripts/with-toolchain.sh cargo clippy -p moss-sim --locked --offline --test month_guide -- -D warnings
```

**Observed:** one reference test passed, 15 filtered out; Clippy passed. The other
15 reference bodies are unchanged from their preceding checks. The revised fence
was formatted with pinned rustfmt, edition 2024.

Two temporary mutants used the same written test:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test meal_snapshot_mutation session_02_order_is_a_rule_not_an_accident -- --exact
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test meal_zero_event_mutation session_02_order_is_a_rule_not_an_accident -- --exact
```

Both compiled and produced the intended assertion failure. The first awarded
each hare from a fresh copy of the initial patch and clamped subtraction from
the real patch; the second hare received 4 instead of 1. Stable sorting remained
in place, so order alone did not fix the accounting. The second mutant recorded
every call, including a zero transfer; the new event-list assertion rejected its
extra `Meal { eater: SimId(7), consumed: 0 }`. The temporary mutants were removed
afterward. The correct extracted target remains available.

The SVG was rendered and visually inspected in Chromium. These checks establish
sequential transfer and outcome-list behavior, not installed eligibility, target
resolution, journal wiring or browser feeding. No live Rust, current exercise,
WASM build or native IDE rendering was changed or rerun.

## Spatial assumptions checkpoint, September 22

A bounded independent review confirmed that cell positions, center-offset rendering,
marker size and future same-cell meals remain consistent. It found a stronger
selection test: the existing axis-aligned candidates could not distinguish
Manhattan distance from straight-line or Chebyshev distance. Session 03 now adds
an explicitly separate fixture at origin (6,6): patch 8 at (9,8) is five cardinal
steps away, while patch 9 at (10,6) is exactly four. At radius 4, only patch 9
qualifies. The selection companion requires this case during future live setup.

A new grid diagram shows the 41 integer cells within Manhattan radius 4. It was
rendered and visually inspected in Chromium. Session 08 now states why equal
endpoints establish equal travel in its particular three-tick, one-step-per-tick
experiment; it no longer implies that endpoints account for arbitrary detours.

The changed session-03 reference was formatted with pinned rustfmt and extracted
into the retained isolated workspace. With its own `CARGO_TARGET_DIR`, these ran:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test month_guide session_03:: -- --nocapture
scripts/with-toolchain.sh cargo clippy -p moss-sim --locked --offline --test month_guide -- -D warnings
```

**Observed:** one reference test passed, 15 filtered out; Clippy passed. The other
15 reference bodies are unchanged from their previous passing checks. No live
helper was implemented or activated.

A temporary `metric_mutation` target substituted squared Euclidean distance and
a squared radius. Its comparison avoids floating-point rounding. With the previous
assertions it compiled and passed one test. Adding the new diagonal assertion
produced one intended failure: actual `Some(SimId(8))`, expected `Some(SimId(9))`.
Both variants used:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test metric_mutation session_03_choices_survive_reordered_observations -- --exact
```

The temporary mutant was removed afterward; the correct extracted target remained
unchanged. This proves that the new fixture distinguishes that alternate metric,
not that every incorrect selection rule is detected. It also does not establish
live query filtering, scheduling or browser choice behavior.

## Ongoing teaching development checkpoint, September 22

Nick clarified that guide development is open-ended. A clean review pass is a
checkpoint, not completion of that goal. Two new audits examined reusable
abilities and the experience of the world. The route now describes adaptable
arcs instead of adding estimates into a fixed course duration. The agent owns
replanning from observed behavior and the point of difficulty actually encountered;
the new [progression reference](../../tutorial/authoring/progression.md) adds no learner tracker.

All sixteen pages now place the live edit, expected result and a diagnostic before
their optional large reference. Repeated runner explanations were shortened.
The ordinary-helper/ECS connection has a source-matched walkthrough and diagram.
Session 12 gains a continuous stock/flow observation; session 15 gains one optional
controlled supply comparison. The hunt remains a recommendation whose timing can
be revisited, with exclusive prey resolution kept explicit. No biological rule
or current exercise was activated.

Two read-only follow-up reviews checked these changes. Corrections named the
travel rate's units, qualified the chosen pursuit/contact design, and aligned
the conceptual map's opening with the adaptable route. The later stock/flow
caption also preserves the distinction between an empty patch at night and
stored food that remains edible. Benefits to learning remain hypotheses until
observed in a real session.

**Checks in this checkpoint:** 641 local links/anchors and fence closure across
72 Markdown files passed, as did `git diff --check`. The initial prose moves
preserved all runnable fences. The later temporal review changed references 11
and 13 and reran the extracted target, as recorded below. All 41 protected
source/configuration/today files still match. New adapter query and call excerpts
match source, ignoring indentation. The two new SVGs and revised arc map were
visually inspected in Chromium; all nine tutorial SVGs parse as XML. Native
RustRover rendering remains unverified.

The day/night diagram's new consecutive-tick calculation was also executed in
the retained isolated reference workspace. `tests/guide_observations.rs` contains
the exact session-12 helpers plus a check starting at reserve 20 and biomass 0:
tick 119 grows/eats 1 and ends at 20/0; tick 120 grows/eats 0 and ends at 19/0.
Each pass spends one upkeep unit. The exact additional command was:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test guide_observations consecutive_day_and_night_can_leave_the_same_empty_patch -- --exact
```

With `CARGO_TARGET_DIR` pointing to that workspace's own `target`, it ran
**one passing test**, with one other reference test filtered out. This checks
the illustrated arithmetic using existing reference helpers. It assumes contact
and omits choice/movement; it does not prove an installed ECS order or a browser
observation. The retained workspace path is recorded below. No live suite or
WASM build was rerun in this documentation checkpoint.

### Rules that meet at a tick boundary

Two more bounded audits examined temporal assumptions and Rust concept transfer.
The guides now explain the initial 119-update daylight interval, future event
stamps using the executing tick, and upkeep requested versus actually deducted.
Session 13 explicitly exposes the proposed no-debt consequence: cost 2, reserve 1
and one new food unit can repeat at reserve 1 throughout daylight. The following
empty night tick is terminal under that proposal. This is a policy to review,
not a new live rule or an accepted nutritional model. Session 15 retains its
valid six-hares-at-cost-1 prediction and limits generalization to higher costs.
Architecture, ecology and the tick-order visual use the same distinction.

Added short Rust explanations beside `split_first`, sorting closures, `and_then`
and the collected owned IDs. An isolated `rust_explanations` test target checks
borrowed/copy types, `None` versus `Some(None)`, and component-independent IDs.
No extra learner exercise or prerequisite was added.

Extracted the current sixteen reference fences into the retained workspace and ran:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test month_guide --test rust_explanations
scripts/with-toolchain.sh cargo clippy -p moss-sim --locked --offline --test month_guide --test rust_explanations -- -D warnings
```

With that workspace's own `CARGO_TARGET_DIR`, **16 reference tests and 3 explanatory
checks passed**; Clippy passed with warnings denied. References 11 and 13 were
formatted with pinned rustfmt. An explicit reset was then added before the
separate cost-2 trace; `cargo test -p moss-sim --locked --offline --test month_guide
session_13:: -- --nocapture`, through the same wrapper, passed one test with 15
filtered out. The executing-event timestamp is a future integration contract,
not exercised by these local references. The edited tick-order SVG was inspected
in Chromium; live browser behavior and RustRover preview were not tested.

### Investigating an apparently wrong inspector result

A further read-only experience review identified a concrete debugging opportunity
in session 04. Its expected two-tick trace now explains why reserve 77 can still
be Seeking: choice read 73 before the meal. The next tick reaches 76, becomes Idle,
clears the target and leaves the patch unchanged. The ECS context links to this
case; the observability plan distinguishes an earlier decision input from a
post-tick snapshot. No new prerequisite or learner task was added.

The retained workspace's `foraging_observation` target combines the exact session
01 and 04 references and adds one arithmetic/state composition test. This command,
using its separate target directory, passed **one test**, with two reference tests
filtered out:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test foraging_observation post_meal_reserve_is_not_the_input_to_the_earlier_choice -- --exact
```

It checks 74→73→77/Seeking, then 77→76/Idle with no second meal, and final biomass 4.
It assumes contact, available food, no competitor and no growth. It contains no
`FoodTarget` adapter; actual target removal, installed ordering and browser
observations remain future activation checks. The diagnostic is an expected
worked case, not a claim that today's inspector has shown those values.

## Review and revision

The lead read the project brief, current code, active checkpoint and document
ownership rules, and used TutorBro's stable-example and executable-increment
methods. One helper first reviewed the sixteen-session sequence and identified
integration tasks too large for a learner's 25-minute block. Two helpers then
drafted disjoint halves and cross-reviewed the other half. The lead read all
sixteen guides, reconciled model choices, and performed a final prose pass.

The revisions made concrete changes to the teaching material: the light-to-growth
conditional remains a learner edit; population verification checks all species
members rather than two samples; growth tests partial remaining capacity and a
request that fully fits; stored food remains edible at night; reset comparisons
account for different run numbers. The lifecycle reference uses actual Bevy
collection/removal while clearly limiting its claim about future scheduling.
The guides add point-of-use explanations for `Option::and_then` and collecting
handles before structural mutation.

Every session contains connected explanation, a remembered earlier idea, one
bounded edit or investigation, required preparation, a worked reference, literal
expectations, a failure diagnostic and a bridge forward. The estimated focus
window excludes substantial agent setup and compiler troubleshooting. That is
an editorial scope assessment, not a measured claim about Nick's reading speed.

### Further teaching refinement, September 22

A subsequent user request asked for sustained research and refinement. Two
read-only audits identified specific learning and technical gaps; the reviewers
then revised disjoint halves and independently reviewed the other half again.
The lead reconciled the older individual-cost chapter and read all revised pages.
[Learning-design research](../../research/learning-design.md) records the primary
sources, their limitations and the resulting editorial choices.

Revisions put short value traces and the learner's edit before long reference
fixtures. Session 03 now explains the candidate decision with `match`; session 06
assigns the default/override resolver, with declarations prepared by the agent.
Sessions 01 and 06 add diagrams that distinguish borrowing existing values from
constructing owned values. The new test-reading context page follows the actual
maintenance regression. The overview now clearly separates a live edit checkpoint
from the optional runner for printed answers.

Technical review also changed the examples. Session 10's future integration
fixture must start seeking without an authored target. Session 13 composes night
and dawn growth with the proposed starvation boundary. Session 14 demonstrates
collection and actual removal without creating a second journal. Session 16
now tests snapshot comparison against different insertion orders, changed energy,
and a changed target; the old wrapper that repeated identical tick calls was
removed. A concrete future dawn/meal/starvation trace replaces that weak capstone.

The consolidation pass then checked older chapters and optional context against
this route. The tutorial home now names one future sequence. Chapter 1 is a
completed source walkthrough; chapters 2, 3 and 1D are integration companions
linked to the canonical exercises. The duplicated selection implementation was
removed, existing headings and legacy anchors were retained, and five marked
Chapter 1 excerpts were compared exactly with current source.

Context now distinguishes maintenance-only observations from travel costs,
meaningful target-component changes from numeric variation, and autonomous ticks
from explicit Reset. Architecture's future ordering agrees with the lessons and
keeps after-meal starvation a proposal. Two final read-only reviews found an
unqualified cost comparison and an assumption that every exercise begins red;
both were corrected. Assertion exercises and investigations can begin green.

## Commands and observed results

From the live repository root:

```sh
python3 docs/tutorial/authoring/check_path_examples.py --keep-workspace
python3 docs/tutorial/authoring/check_path_examples.py --session 1
```

The first command extracts one complete tagged Rust module from each session,
copies the current pinned Cargo workspace into a temporary directory, and runs:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test month_guide
```

Its `CARGO_TARGET_DIR` points to that copy's own `target` directory. **Observed:
16 tests passed, zero failed, zero ignored.** An initial complete pass preceded
the independent review refinements; a final complete pass checked the
code examples after those changes, including nondefault light-to-growth rates. That initial edition's retained reference workspace was
`/private/var/folders/dc/n3zh722n3_s3tfb4806dh9mh0000gn/T/moss-path-examples-zwbb3fkm`.
The further refinement was extracted and tested in
`/private/var/folders/dc/n3zh722n3_s3tfb4806dh9mh0000gn/T/moss-path-examples-1o4zl3h_`:
**16 passed, zero failed, zero ignored**, including a fractional-mean check.
The revised target also passed the Clippy command below. These copies are
disposable evidence, not development checkouts for Nick to work in.

The single-session command extracted session 01 and ran **exactly one passing
test**. Its temporary workspace was removed by the script afterward. `--help`
was also checked. Examples use cached dependencies; a fresh machine still needs
the normal toolchain/dependency preparation.

The final extracted test target also passed:

```sh
scripts/with-toolchain.sh cargo clippy -p moss-sim --locked --offline --test month_guide -- -D warnings
```

That command ran inside the retained copy with its separate target directory.
All sixteen Rust fences were formatted with pinned `rustfmt` 1.93.1, edition
2024. Cargo used Moss's pinned Rust 1.93.1 and Bevy ECS 0.18.1 from the existing
lockfile; no dependency versions changed.

Seven deliberately faulty variants were tried separately in the refinement's
isolated test target. Each compiled and reached its intended test assertion;
each produced **one failed test**, rather than a setup failure. Each variant
started from the original text, which was restored at the end.

| Reference variant | Expected defect detected |
| --- | --- |
| 02: omit stable sorting | Reversed request order changes the rewarded identity. |
| 03: compare ID before distance | A farther low-ID patch wins. |
| 05: use integer division | A mean of 20.5 is truncated. |
| 06: filter out a zero override | `Some(0)` incorrectly receives the species default. |
| 13: omit dawn production | The dawn meal no longer rescues Fern. |
| 14: report candidates without removal | The next call reports the same creatures again. |
| 16: replace every present target with ID 3 | A change of destination disappears from the comparison. |

Each used `cargo test -p moss-sim --locked --offline --test month_guide
session_NN::` through `scripts/with-toolchain.sh`, in the same isolated workspace
and target directory. This is targeted evidence that these example assertions
discriminate between the intended behavior and specific mistakes. It is not
general mutation coverage, learner-outcome evidence, or live schedule verification.

The exact command in the new [test-reading page](../../tutorial/context/reading-a-test.md)
also ran in the live repository: `maintenance_spends_energy_and_stops_at_zero`
passed **one test**, with one other test filtered out. No source edits were needed.

## What each reference establishes

| Session | Tested scope, and where the claim stops |
| --- | --- |
| 01 | Bounded energy/biomass transfer, full capacity and zero bite. No eligibility or ECS adapter. |
| 02 | Stable-ID ordering of already eligible eaters, shared remainder and actual meal versus net change. No ECS eligibility query. |
| 03 | Local, nonempty nearest-food selection with stable ties, meaningful absence, an inclusive radius boundary and a diagonal metric discriminator. No installed target mutation. |
| 04 | Idle/Seeking transitions and threshold boundaries using post-maintenance reserves. No deferred target visibility check. |
| 05 | Count, minimum, maximum and mean, including a fractional mean; empty input stays absent. Species filtering and browser aggregates remain future checks. |
| 06 | Copy ownership, default/override resolution and explicit zero. No live spawn wiring. |
| 07 | Actual Bevy component attachment and maintenance query in an isolated schedule, including clamping at zero. No future complete-world reset acceptance. |
| 08 | Accounting for supplied affordable movement traces, equal travel and unequal upkeep. The reference does not move creatures. |
| 09 | Growth reports the actual amount, handles partial capacity, honors a request that fits and allows cropped patches to regrow. No installed growth system. |
| 10 | Growth-before-meal arithmetic and full-patch ordering. No autonomous-choice or live-schedule proof. |
| 11 | Day/night boundaries, executing versus completed tick, and initial versus full daylight opportunity counts. No environment resource or browser time check. |
| 12 | Light changes production, capacity still limits it, and stored food remains edible at night. No live environment adapter. |
| 13 | Proposed after-meal starvation with stored night food, an empty night, dawn growth, and repeated underpaid upkeep through daylight. Contact is assumed; no installed choice or accepted death policy. |
| 14 | Actual Bevy collection/despawn, ordered removed IDs, repeat-call behavior and an unchanged surviving control. No journal or full later-action schedule check. |
| 15 | Count-account consistency and rejection of impossible deaths or counter overflow. Supplied samples do not establish real population dynamics or supply. |
| 16 | Canonical component snapshots match across insertion orders and detect changed reserves and targets. No tick execution, browser repeatability or full future-state comparison. |

## Navigation, visuals and preservation

The three new local SVGs were rendered and visually inspected in in-app Chromium:
the four-arc map, the two-threshold activity diagram and the proposed full-tick
order. All labels fit their panels. The SVGs also parse as XML. Markdown uses
ordinary headings, relative links, fenced code and two-column tables. RustRover's
own Markdown preview remains unverified. A later background accessibility check
reached its existing source window, but slow access prevented a reliable preview
check; no editor configuration was changed.

The refinement added two more SVGs: the meal's before/during/after borrow and
the default/override-to-owned-component flow. Both were rendered and inspected
in Chromium, and all five path SVGs parse as XML. A missing arrowhead on one
meal path was corrected and the result re-inspected. The visuals illustrate
the worked examples; they are not screenshots of activated biology.

Before writing, SHA-256 hashes captured 41 existing files: `NOW.md`, today's
guide, its movement/meal chapters, all current crate files, runtime scripts,
shared IDE actions and Cargo/toolchain files. After writing, **all 41 match**.
The source-preservation check distinguishes this documentation work from earlier
uncommitted runtime changes already present in the worktree.

The final check passed **625 local file/heading links** and fence closure across
**71 Markdown files**, plus `git diff --check`. The current sixteen reference
fences exactly match the restored, passing isolated target. The temporary diagram
preview and its server were closed; the existing Moss browser tab was retained.
External optional Rust/Bevy references
were checked against the official Rust book, standard-library docs and versioned
Bevy ECS 0.18.1 Query documentation. These references support API explanation;
the executed examples establish the particular code combinations above.

## What remains for future activation

Today's live helper test remains intentionally red and movement stays unscheduled.
This pass did not rerun the live full suite, rebuild WASM, or claim new browser
behavior. The existing runtime coverage remains in
[development verification](../../development/verification.md).

Before each later session, the agent must inspect current code, prepare the named
plumbing and a focused live test, then offer the one learner edit. After review,
verify the installed order, Reset and browser observation that the guide names.
Preserve the literal checks already earned; update older isolation fixtures when
new rules make their previous assumptions inappropriate. A future reference
passing today is preparation for that work, not evidence it is complete.

The recorded reviews are checkpoints in ongoing teaching development. Their
checks support consistency and examples; they do not measure the experience of
working through the guide as a learner. Continue examining the explanation and
model assumptions while preserving today's actual movement edit. When real
session feedback arrives, use the point where Nick needed help to guide the next
revision rather than assigning more preparatory reading.

## Three meal limits checkpoint, September 22

Refined session 01's transition from movement after a bounded read-only review.
Its previous link now points to v2, and the prose explains that the helper's
energy/patch arguments cannot establish contact, species or target eligibility.
Those checks belong to the later ECS adapter. The helper body, active movement
exercise, meal companion chapter and session 02 remain unchanged.

The old reference checked room-limited transfer, a full animal and zero bite.
It did not make finite supply or a configurable positive bite determine the
result. Extended the same example after its explicit test-fixture reserve reset:
from reserve 0 and biomass 3, bite 2 awards 2 and leaves 1; bite 4 then awards
only that 1; another bite awards nothing. Literal assertions check both state
changes and the returned amount. The new prose follows that trace without
assigning another edit. The final independent prose review found no issues.

Used the retained isolated workspace
`/private/var/folders/dc/n3zh722n3_s3tfb4806dh9mh0000gn/T/moss-path-examples-1o4zl3h_`.
Before testing, compared all eight copied simulation source files with live
source; they match. Extracted the old and revised reference fences into scratch
test targets. The two controlled faulty variants changed only the amount:

- Omitting `min(patch.biomass)` ignores available supply.
- Keeping zero bite special but substituting 4 for every positive limit ignores
  the caller's configured bite.

With this copy's own target directory, ran:

```sh
CARGO_TARGET_DIR="$PWD/target" scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test meal_before_no_supply --test meal_before_fixed_bite
CARGO_TARGET_DIR="$PWD/target" scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test meal_reference
CARGO_TARGET_DIR="$PWD/target" scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test meal_after_no_supply
CARGO_TARGET_DIR="$PWD/target" scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test meal_after_fixed_bite
CARGO_TARGET_DIR="$PWD/target" scripts/with-toolchain.sh cargo clippy -p moss-sim --locked --offline --test meal_reference -- -D warnings
scripts/with-toolchain.sh rustfmt --edition 2024 --check crates/moss-sim/tests/meal_reference.rs
```

Both faulty variants **passed the old test**, one passing test in each target.
The revised correct reference passed one test, with clean Clippy and formatting.
Both faulty variants compiled and then failed the revised test: the missing
supply limit triggered checked subtraction overflow; the fixed bite returned 3
where the assertion expected 2. Each failed target exited 101. This is targeted
evidence for two missing discriminators, not general mutation coverage. Removed
the four owned mutant targets afterward. The pyenv-shims notice did not prevent
checks.

The other fifteen reference bodies are unchanged. This focused run does not
repeat the earlier full sixteen-reference suite or prove ECS eligibility,
installed meal order, WASM, browser behavior or RustRover rendering. The meal
remains future learner-owned work, and today's next edit is still movement.

Navigation validation passed **732 local destinations across 75 Markdown files**;
`git diff --check` passed. Of 43 snapshotted source/lesson files, only session 01
and its evidence summary changed as intended. The original 41-file protection
set also matches, and the meal helper body is byte-identical to its prior answer.


## Requiring reference tests to execute, September 22

The path runner previously accepted Cargo's exit status after checking for the
literal source text `#[test]`. Reproduced a false green by calling that version's
`run_checked` on two isolated test targets: an ignored test reported zero passed,
one ignored; a `#[cfg(any())]` test produced zero tests. Both commands returned
zero and the old check accepted them. These were reproductions of its execution
check, not full invocations of the old `main` function.

The runner now uses compiled `--list` output, requires a test in each selected
`session_NN` module, then requires every discovered name to report passing under
normal execution. It does not enable ignored path tests. The shared result
parser accepts libtest's exact expected-panic suffix. The official
[Rust test-harness documentation](https://doc.rust-lang.org/rustc/tests/index.html)
was checked for listing, ignored tests and stable pretty output; no dependency
or toolchain changed.

Native probes used the retained isolated workspace
`/private/var/folders/dc/n3zh722n3_s3tfb4806dh9mh0000gn/T/moss-path-examples-1o4zl3h_`
with its own target directory. Each probe temporarily replaced that copy's
`month_guide.rs`; its previous bytes were restored in `finally`. Called
`run_path(workspace, env, [1])`, which executes:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test month_guide -- --list --format pretty --color never
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test month_guide -- --format pretty --color never
```

The ignored test was discovered but rejected after reporting ignored. The
conditionally excluded test was rejected at discovery; execution did not follow.
The expected-panic probe discovered and passed one test with the actual output
`test session_01::must_run - should panic ... ok`. Owned probe targets were
removed after checking; the retained workspace's original reference was restored.

From the live repository, the updated runner and its fixtures were checked with:

```sh
python3 -B -m unittest discover -s docs/tutorial/authoring -p 'test_check_path_examples.py' -v
python3 -B docs/tutorial/authoring/check_path_examples.py
python3 -B docs/tutorial/authoring/check_path_examples.py --session 01
```

Seventeen Python fixtures passed, including failed discovery, missing modules,
ignored or missing results, expected panic, selected-session isolation and
cleanup after success or failure. The default native run discovered and passed
all sixteen current references; the selected-session run discovered and passed
one test. Neither had ignored tests. Both disposable workspaces were removed by
the runner. A bounded read-only review found no actionable defects.

This proves the named compiled references executed successfully. It cannot prove
that assertions are sufficient or infer which additional tests excluded by
conditional compilation were intended. Movement-mode native checks, path Clippy,
WASM and browser/IDE checks were not repeated in this pass. No printed answer,
live source, schedule or learner assignment changed.

Navigation validation passed **743 local destinations across 75 Markdown files**;
`git diff --check` passed. Of 43 source/lesson files snapshotted before this pass,
only the path evidence summary changed. All live Rust files, NOW, v2, the original
lesson, companion chapters and sixteen reference lessons match their snapshots.


## Equal values, different authored inputs, September 22

A bounded read-only review of the individual-cost route found one missing
example: `Some(1)` and `None` resolve equally when the species default is 1,
but remain different authored instructions. Added that case to session 06's
existing reference and explained it in the attributes context. Constructing new
values under test default 9 gives 9 for absence and 1 for the explicit override.
The context's table distinguishes that new configuration from ordinary Reset
with unchanged authored inputs. A resolved number alone cannot establish its
origin; future inspection must use recorded provenance or say unknown.

The resolver, component declaration and live source did not change. The new
assertions demonstrate resolution, not scenario retention, installed Reset or a
new provenance mechanism. Preserved explicit zero and existing ownership checks.
Also repaired the applied-research note's obsolete declaration-only assignment:
it now leads to current movement and the future resolver/query sessions. External
research was not re-fetched; its earlier source-review date remains unchanged.

From the repository root, ran:

```sh
python3 -B docs/tutorial/authoring/check_path_examples.py --session 06 --keep-workspace
```

The runner discovered and passed exactly one reference test, with none ignored.
It retained the isolated workspace
`/private/var/folders/dc/n3zh722n3_s3tfb4806dh9mh0000gn/T/moss-path-examples-txcizpk4`.
With its own target directory and the copied pinned toolchain/lockfile, ran:

```sh
scripts/with-toolchain.sh cargo clippy -p moss-sim --locked --offline --test month_guide -- -D warnings
```

Clippy passed. The extracted printed fence also exactly matched pinned rustfmt
stdout using edition 2024. The other fifteen references were not rerun, and no
WASM, browser, IDE or live full-suite check was performed. Today's learner-owned
movement rule remains unfinished and unscheduled.

The final independent prose/test review found no actionable issues. Navigation
validation passed **758 local destinations across 75 Markdown files** and
`git diff --check` passed. Of 43 snapshotted source/lesson files, only session 06
and its evidence summary changed. The resolver body is byte-identical to the
checkpoint; current source, NOW and today's lesson match the pre-review snapshot.

## Query membership checkpoint, September 22

A bounded read-only review found that session 07's original test established
attachment and individual arithmetic, but did not distinguish a query with or
without `With<Creature>`. Its grass entity lacks both requested data components,
so either declaration skips it. The existing ECS context's access and disjointness
explanations needed no change.

Added a separate supplied reference test with three entities at reserve 60:
an eligible animal with cost 1, an animal missing costs, and a non-animal with
costs and energy. One pass leaves 59, 60 and 60 respectively. The positive control
prevents an unscheduled system from satisfying the unchanged-value assertions.
The guide now separates a deliberately incomplete diagnostic from the requirement
that valid spawned animals carry costs. A two-column table exposes the inputs and
expected outputs before the code. This adds no live assignment; the learner's
future edit remains the maintenance query, and today's task remains movement.

Used TutorBro's concrete-example and connected-prose guidance. Checked the pinned
[Bevy query documentation](https://docs.rs/bevy_ecs/0.18.1/bevy_ecs/system/struct.Query.html#query-filtering)
for the distinction between requested data and a presence filter. No dependencies,
live source, schedule, shared run configuration or authored scenario changed.

Ran from the repository root:

```sh
python3 docs/tutorial/authoring/check_path_examples.py --session 7 --keep-workspace
```

The execution guard discovered and passed **two** tests with none ignored. The
isolated workspace was retained at
`/private/var/folders/dc/n3zh722n3_s3tfb4806dh9mh0000gn/T/moss-path-examples-1jelcra_`.
It used its own target directory and the copied pinned Rust 1.93.1/Bevy ECS 0.18.1
workspace. Checked the extracted fence with pinned rustfmt, corrected one line
wrap, then passed formatting and:

```sh
scripts/with-toolchain.sh cargo clippy -p moss-sim --locked --offline --test month_guide -- -D warnings
```

In that scratch target only, removed `With<Creature>` from the reference system.
The original `session_07_same_species_can_have_different_costs` still passed.
The new `session_07_only_matching_entities_pay` failed: the non-animal's actual
reserve was 59, expected 60. Restored the correct reference in a `finally` block
and reran the target: **two passed**, zero ignored. The old positive test and
system body remain unchanged in the guide; only the new test detects this removed
filter. This checks one specific defect, not every possible query mistake.

Independent review confirmed the teaching distinction and identified a table
width issue, corrected to the authoring contract's two columns. No full path
target, live suite, WASM build, browser check or native IDE preview was rerun.
The sixteen session references now contain seventeen tests; the last full-target
result above remains its historical sixteen-test result, with this focused check
covering the addition.

Final checks on September 23 passed 761 local destinations across 75 Markdown
files and `git diff --check`. All 29 protected Rust source, shared configuration,
NOW and v2 lesson files matched the pre-pass hashes. The existing reference body
and positive test remained byte-identical; the membership test was appended.
