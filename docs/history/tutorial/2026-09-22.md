# Tutorial verification — September 22, 2026

[Archived evidence summary](https://github.com/NicholasHazen/moss/blob/015ecdbf76a4c5253df8e1b182c0bee84c0cca9e/docs/tutorial/authoring/verification.md) · [Work history](../README.md)

These dated records retain the commands, observations and limits of each
checkpoint. Counts and references to “current” or “today” describe that entry's
stage; later entries can supersede earlier ones. Temporary workspaces are
disposable evidence, not required development checkouts or retained test tools.
Moving this record did not rerun its runtime checks.

## Today's lesson v2, September 22, 2026

Nick requested a second version that applies the teaching refinements. Added
`today-v2.md` as a self-contained route through the same movement exercise:
initial test, an affordable/rejected trace, a complete worked answer, targeted
Rust reminders, test diagnostics, an ECS review walkthrough and the expected
browser journey. Reused two previously inspected SVGs. Two bounded reviewers
checked learning flow and technical accuracy; removed two distracting side
comparisons after review. No additional prerequisite or learner edit was added.

`NOW.md`, the project/tutorial entry points, future-path navigation and RustRover
reading instructions now lead to v2. The original `today.md` and Chapters 4–5
remain unchanged. `NOW.md` changed only its two lesson links; the active task,
run command and stopping point are the same. The authoring index records which
version is current. Reading time and learning gains have not been measured.

Ran the v2 opening command in the **live workspace**:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --test movement movement_charges_only_an_affordable_actual_step -- --exact
```

It ran **one expected failure** at `move_one_cell`'s unchanged `todo!()`, with two
tests filtered out. This verifies the starting checkpoint; it does not claim
movement works live. The v2 `movement-v2-helper` fence and Chapter 4's answer
match after pinned rustfmt, and the v2 fence is formatted accordingly.

In the retained isolated workspace
`/private/var/folders/dc/n3zh722n3_s3tfb4806dh9mh0000gn/T/moss-path-examples-1o4zl3h_`,
inserted the exact v2 fence into the copied helper. With its own
`CARGO_TARGET_DIR`, the following focused command passed **one test**:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test movement movement_charges_only_an_affordable_actual_step -- --exact
```

Then, in that copy only, installed maintenance → movement → completion and
removed the two activation ignores. These checks passed **five tests, none
ignored**, and Clippy with warnings denied:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test movement --test maintenance
scripts/with-toolchain.sh cargo clippy -p moss-sim --locked --offline --test movement --test maintenance -- -D warnings
```

Finally restored the isolated helper, schedule and test files byte-for-byte to
the live baseline. No live Rust, test, schedule, dependency or IDE action changed.
No full workspace suite, WASM build, new browser journey or native RustRover
Markdown rendering was run. The browser table remains post-activation acceptance;
the two reused diagram renders retain their earlier evidence.

Final validation passed 679 local destinations across 73 Markdown files and
`git diff --check`. Forty of the 41 previously protected files are unchanged;
reversing only NOW's two authorized link edits reproduces its original hash.
All sixteen future reference fences are unchanged. The v2 answer matches its
formatted, executed extraction, and the adapter query excerpt matches source.

## History coverage checkpoint, September 22, 2026

The observability plan and sessions 14–15 now explain partial retention within
one tick and distinguish retention from collection. A 32-entry journal can lose
three of 35 same-tick events while still naming that tick as its oldest retained
timestamp. Zero evictions does not mean every change was recorded; a global
eviction count cannot reconstruct an exact death count. Current journal entries
have no separate event IDs, and the plan now labels those IDs as future work.

A bounded independent review informed the explanation. Two new isolated checks
using unchanged simulation source and the included journal unit test passed,
as did Clippy. [Path evidence](https://github.com/NicholasHazen/moss/blob/015ecdbf76a4c5253df8e1b182c0bee84c0cca9e/docs/tutorial/path/verification.md#history-coverage-checkpoint-september-22)
records exact commands and limits. No source, active exercise or runnable lesson
reference changed. No future death counters, browser behavior or IDE rendering
were tested or activated.

The final read-only review found no further issues. Navigation validation passed
658 local destinations across 72 Markdown files, and whitespace checks passed.
All 41 protected source/configuration/today hashes and all sixteen runnable
reference fences remain unchanged.

## Test diagnostics checkpoint, September 22, 2026

Expanded the optional test-reading context with a short diagnostic table and a
specific tail-semicolon explanation. It distinguishes a Cargo integration-test
target from a test that actually executes the ECS schedule. The context index
links directly to the diagnostic section. A bounded independent review informed
the changes; no additional learner task was assigned.

In the retained isolated workspace
`/private/var/folders/dc/n3zh722n3_s3tfb4806dh9mh0000gn/T/moss-path-examples-1o4zl3h_`,
`movement_diagnostic.rs` contains the exact Chapter 4 helper plus one affordable
step assertion. With the copy's own `CARGO_TARGET_DIR` and pinned Rust 1.93.1:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test movement_diagnostic diagnostic_affordable_step -- --exact
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test movement_diagnostic diagnostic_affordable_stpe -- --exact
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test movement movement_charges_only_an_affordable_actual_step -- --exact
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test movement movement_tick_resolves_target_and_reset -- --exact
```

The first command was run with an intentionally added final semicolon, then with
the exact correct helper restored. It first failed compilation with E0308,
expected `u32`/found `()`, and then passed **one test**. The deliberately misspelled
filter in the second command exited successfully with **zero tests, one filtered
out**. The copied live stub produced **one intended panic** at its `todo!` with
the documented message. The selected activation check reported **zero passed,
one ignored** and did not execute its body. Its ignore attribute remains intact.
The isolated source containing the stub was byte-compared with live `lessons.rs`.

Consulted the official [function-return explanation](https://doc.rust-lang.org/book/ch03-03-how-functions-work.html#functions-with-return-values),
[Cargo test reference](https://doc.rust-lang.org/cargo/commands/cargo-test.html)
and [`todo!` reference](https://doc.rust-lang.org/std/macro.todo.html).
The maintenance assertion example is explanatory; its earlier live evidence was
not rerun here. No live rule, current exercise, WASM build, browser observation
or native RustRover run was changed or performed in this pass.

The final read-only review found no further issues. Navigation validation passed
656 local destinations across 72 Markdown files and `git diff --check` passed.
All 41 protected source/configuration/today hashes match; all sixteen reference
fences still match the previously tested isolated target.

## Shared food and actual outcomes, September 22, 2026

Session 02 now contrasts an earlier observation with the current patch used for
each transfer. A new diagram shows how sequential, valid Rust borrows can still
over-award from stale values. The reference adds a completely depleted patch:
the second eligible contender receives zero and creates no meal record. Session
03 links its observed candidates to this later resolution without adding an edit.

The revised reference and Clippy passed in isolation. Two deliberately faulty
variants compiled and failed the intended reward/event assertions. A bounded
independent review informed the changes; the diagram was inspected in Chromium.
[Path evidence](https://github.com/NicholasHazen/moss/blob/015ecdbf76a4c5253df8e1b182c0bee84c0cca9e/docs/tutorial/path/verification.md#shared-outcomes-checkpoint-september-22)
records exact commands and scope. Live eligibility, schedule, journal and browser
feeding remain unimplemented and unverified. Today's exercise is unchanged.

Final navigation validation passed 650 local destinations across 72 Markdown
files, and `git diff --check` passed. All twelve SVGs parse; all 41 protected
source/configuration/today hashes match. The sixteen current reference fences
exactly match the isolated target. The review of the final prose and SVG source
found no further issues; native RustRover rendering remains unverified.

## Reproducible navigation checks, September 22, 2026

Added `check_docs.py` and ten focused fixtures under `authoring/`, replacing
reliance on a temporary checker. A bounded helper implemented the checker; the
lead supplied independent fixtures and reviewed its supported Markdown subset.
Fenced examples no longer supply real headings or aliases, and explicit reference
uses are checked against their definitions. The authoring guide and document
index now name the retained command. Updated the IDE reading page's stale count
of diagrams without changing its navigation instructions.

These commands passed from the repository root:

```sh
python3 -B -m unittest discover -s docs/tutorial/authoring -p 'test_check_docs.py'
python3 docs/tutorial/authoring/check_docs.py
```

The fixture suite passed **10 tests**. The repository scan checked **647 local
destinations across 72 Markdown files**. Running the script by absolute path
from `/private/tmp` also found the correct root and passed; `--help` documented
the scope. A fixture confirms broken navigation produces a nonzero CLI exit.
The destination count checks reference definitions once rather than counting
each use; it is not directly comparable to earlier temporary-script totals.

This tool does not fetch external URLs, establish complete Markdown conformance,
compile Rust examples or verify RustRover rendering. No live Rust, WASM build or
browser behavior was rerun. All 41 protected source/configuration/today hashes
and the sixteen reference fences remain unchanged; whitespace validation passed.

## Spatial reasoning and test coverage, September 22, 2026

A bounded spatial audit found no conflict between simulation cells, marker centers,
click targets and planned contact. It did find that the selection reference used
only axis-aligned candidates. Added one diagonal case and a reach diagram to
session 03, named that case in the integration companion, and qualified session
08's inference from equal endpoints to travel. The exercise sequence is unchanged.

The correct revised reference passed its focused check; Clippy passed. A temporary
straight-line-distance variant passed the earlier assertions and failed the new
case as intended. [Path evidence](https://github.com/NicholasHazen/moss/blob/015ecdbf76a4c5253df8e1b182c0bee84c0cca9e/docs/tutorial/path/verification.md#spatial-assumptions-checkpoint-september-22)
records commands, exact outcomes and limits. The diagram was visually inspected
in Chromium and its shaded integer-cell count checked. No new live behavior,
WASM build or native RustRover preview was tested.

Static validation passed 647 local links/anchors and fence closure across 72
Markdown files and `git diff --check`. All eleven SVGs parse; all 41 protected
source/configuration/today hashes match. Current reference fences match the
isolated target exactly.

## Component-boundary comparison, September 22, 2026

Expanded the optional [attributes context](https://github.com/NicholasHazen/moss/blob/015ecdbf76a4c5253df8e1b182c0bee84c0cca9e/docs/tutorial/context/attributes-and-defaults.md#where-would-one-more-attribute-live)
with a hypothetical upkeep/pace comparison. Both candidate layouts retain owned
individual values; splitting components narrows declared access and permits
independent presence. Existing `Energy { reserve, capacity }` provides the
counterexample to one scalar per component. A new SVG compares the layouts;
session 06 links to it without adding an assignment. Research links moved after
the worked explanation. Corrected a stale ecology filename in the parking lot.

A bounded independent review informed the example and checked the final prose
against official Bevy ECS 0.18.1 Query documentation and cached crate source.
Its correction distinguishes lacking a locomotion capability from merely resting.
The lead inspected the diagram in Chromium. Native RustRover preview remains
unverified. These hypothetical types and movement pace are not installed.

In the retained isolated workspace
`/private/var/folders/dc/n3zh722n3_s3tfb4806dh9mh0000gn/T/moss-path-examples-1o4zl3h_`,
added `tests/component_boundaries.rs` to the simulation package for these checks:

- Grouped upkeep reads conflict with a pace write on the same component;
  split costs/locomotion accesses are compatible. Two Energy writers still conflict.
- Missing locomotion does not exclude an animal from upkeep; deliberately missing
  required costs does. Optional locomotion inspection preserves that distinction.
- Different numeric costs, including a later field edit, keep the same archetype.
- A nested `Locomotion` field does not become an independently attached component.

With `CARGO_TARGET_DIR` set to that copy's own `target`, these commands passed
**four tests** and Clippy with warnings denied:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test component_boundaries
scripts/with-toolchain.sh cargo clippy -p moss-sim --locked --offline --test component_boundaries -- -D warnings
```

The scratch example was formatted with pinned rustfmt, edition 2024. These checks
establish component-access and storage semantics, not scheduler throughput or a
Moss performance result. No live Rust, exercise, scenario or schedule changed;
the sixteen lesson references remain unchanged. No live test suite, WASM build
or new browser behavior was exercised in this pass.

Static validation passed 645 local links/anchors and fence closure across 72
Markdown files, plus `git diff --check`. All ten tutorial SVGs parse as XML; all
41 protected source/configuration/today hashes match. The sixteen reference
fences exactly match the previously tested isolated target.

## Ongoing teaching development, September 22, 2026

The goal is now explicitly open-ended. Added an agent-facing capability and
adaptation plan, moved the live checks ahead of optional long references in all
sixteen sessions, and made the proposed arcs responsive to observed behavior.
Added a source-matched ECS adapter walkthrough, two diagrams, and one controlled
scarcity comparison. Corrected the remaining stale module export in the Rust
context page. Two independent audits and follow-up reviews informed the changes.

A temporal review then exposed upkeep underpayment under the proposed starvation
policy, clarified initialization versus executed daylight, and specified future
event timestamps. The two affected references were revised: all sixteen reference
tests and three isolated Rust-explanation checks passed, along with Clippy. A final
explicit fixture reset in reference 13 passed its focused rerun. The earlier
stock/flow diagram check also passed. None establishes live future behavior.

The new diagrams, revised arc map and corrected tick-order diagram were inspected
in Chromium. Static checks passed 641 links/anchors and fence closure across 72
Markdown files; all nine SVGs parsed and whitespace validation passed. All 41
source/configuration/today preservation hashes match. No full live suite, WASM
build or native RustRover preview was rerun. [Path evidence](https://github.com/NicholasHazen/moss/blob/015ecdbf76a4c5253df8e1b182c0bee84c0cca9e/docs/tutorial/path/verification.md)
records the exact scope. This is a refinement checkpoint, not goal completion.

A subsequent experience review added one two-tick foraging investigation to the
existing lesson: post-meal reserve is not the input to the earlier choice. Its
isolated composition check passed one test; installed target removal and the
browser observation remain future checks. This teaches choosing an observation
without assigning an extra debugging chapter.

## Research-led teaching refinement, September 22, 2026

Revised all sixteen short-session pages after two focused audits and independent
cross-reviews. Added concrete traces before dense references, two ownership
diagrams, and an optional walkthrough of the current maintenance test. Session 06
now teaches the default/override resolver; its type and exports are agent
preparation. Chapter 1D was reconciled to that route. Its old component/export
execution evidence below describes the earlier proposal, not the newly proposed
public resolver wiring.

Corrected growth/starvation continuity, strengthened the future growth-before-choice
fixture, simplified removal without duplicating history, and replaced the weak
batching example with discriminating snapshot checks and a future dawn capstone.
The [research note](../../research/learning-design.md) separates study findings
from our proposed teaching design. No learning gain or timing has been measured.

All sixteen revised reference tests and Clippy passed in an isolated workspace.
Seven deliberate faulty variants compiled and failed their intended assertions;
the original references were restored. The test-reading page's exact live
maintenance command ran one passing test. The two new SVGs were visually checked
in Chromium; RustRover preview was not operated. All 41 captured source,
configuration and today's-session files remain unchanged. [Detailed path evidence](https://github.com/NicholasHazen/moss/blob/015ecdbf76a4c5253df8e1b182c0bee84c0cca9e/docs/tutorial/path/verification.md)
records the scope and remaining future activation work.

The consolidation pass made the short-session path the single future sequence.
Older chapters now provide completed walkthroughs or integration context; optional
pages explain concepts without assigning a competing route. Removed the duplicate
food-selection answer and reconciled architecture's proposed starvation ordering.
Five marked Chapter 1 excerpts exactly match current source. Two final independent
reviews yielded two small clarifications, both applied. The final check passed
625 local links/anchors and fence closure across 71 Markdown files and whitespace
validation. All sixteen references still exactly match the passing isolated target;
all 41 preservation hashes still match. No further runtime check was needed for
these prose changes. A slow RustRover accessibility attempt did not verify Markdown
preview; the temporary diagram browser/server were closed afterward.

## Week-to-month short sessions, September 22, 2026

Added [sixteen connected guides](https://github.com/NicholasHazen/moss/blob/015ecdbf76a4c5253df8e1b182c0bee84c0cca9e/docs/tutorial/path/README.md) after today's session, with
short-session pacing, repeated concepts, complete worked references and three
local diagrams. Two helpers drafted disjoint halves and independently reviewed
the other half; the lead reconciled the teaching route, ownership and examples.

All sixteen extracted examples passed in an isolated workspace, including a
second pass after review improvements; the exact single-session command ran one
passing test. The extracted target passed native Clippy with warnings denied.
[Path evidence](https://github.com/NicholasHazen/moss/blob/015ecdbf76a4c5253df8e1b182c0bee84c0cca9e/docs/tutorial/path/verification.md) records each example's scope, commands,
review changes and remaining future integration checks.

Hashes confirm today's guide, movement/meal chapters, `NOW.md`, and all 37 other
captured source/configuration files are unchanged (41 files total). The three
new SVGs rendered cleanly in Chromium; RustRover's own preview was not operated.
Live runtime tests and WASM/browser behavior were not rerun for this documentation
work. Local links, anchors and fences and `git diff --check` passed.

## Movement session preparation, September 22, 2026

Replanned from `86f9f2e` plus Nick's uncommitted maintenance work and subsequent
module/document organization. TutorBro's stable-example and executable-increment
patterns now lead to one visible journey. Added `today.md` and two SVG diagrams;
reordered the chapter navigation without deleting existing chapter anchors.
Individual costs, populations and autonomous choice are no longer prerequisites
for the first movement. Later chapters retain explicit preparation requirements.

Live scaffolding adds `FoodTarget`, `MovementRules`, fixture intent, the
unscheduled adapter, movement test target, inspector facts and an IDE Run action.
`move_one_cell` remains `todo!()`. The exact focused command runs **one expected
failure**, not zero matches. Seven existing simulation tests and five browser
support tests pass. The two installed-movement regressions remain deliberately
ignored until helper review. Maintenance assertions are preserved; their setup
removes food to isolate that rule from later travel/eating.

Copied manifests, lockfile, scripts and crate sources to
`/private/tmp/moss-today-fdz47wii`, with its own `CARGO_TARGET_DIR`. Inserted the
exact `movement-helper` fence from Chapter 4 into that copy only. Running
`scripts/with-toolchain.sh cargo test --workspace --locked --offline` passed
13 tests with two ignored while movement remained unscheduled. Then chained
maintenance → movement → complete tick and removed **both** activation ignores
in that copy. The same command passed **15 tests, zero ignored**: ten simulation,
five presentation. This exercises arrival, continued maintenance, reset, target
resolution, and rejected movement. Live biology was not completed by the agent.

The Chapter 4 test fence is labeled as the first cases of the installed test;
the live test also covers directions, exact affordability and invalid starts.
Chapter 5's unchanged worked meal helper/test retain their historical native
verification; no meal adapter, schedule or browser behavior was validated today.
The meal's tick-9 arrival numbers and same-cell example are acceptance targets.

Finally inserted Chapter 1D's exact proposed component and updated export into
the same isolated copy (after successful movement activation); all 15 tests
still passed. This only checks that the future declaration/export compiles while
preserving `MovementRules`. No animal owns or reads that component yet.

The live release bundle built and passed the limited maintenance browser smoke
check detailed in [build evidence](../builds/2026-09-22.md#movement-session-preparation--september-22-2026).
Both SVG diagrams were visually inspected in Chromium. RustRover Markdown preview
and its new Run action were not operated; the action's command was run in a terminal.
Actual browser movement awaits the learner edit and agent activation.

Static checks passed: 451 local file/heading links and fence closure across
51 Markdown files, both SVGs and the new IDE XML configuration parsed, and
`git diff --check` passed. Existing chapter anchors were preserved.

Older entries below describe their checkpoint at the time, not today's active edit.

## Documentation organization, September 22, 2026

Separated current documentation, agent conventions and dated history. Updated
relative links after moving design/development/research documents, shortened
reader navigation and authoring instructions, and retained chapter filenames
and heading anchors. The current checkpoint and learner ownership are unchanged.

Compared all six coding chapters against the starting worktree: their 22 Rust
and shell fences are byte-identical, and every heading anchor is preserved.
Checked 410 local file/heading links and closed fences across 50 Markdown files;
`git diff --check` also passed. Runtime tests, browser
checks and worked-example execution were not repeated for these documentation
changes; the recorded module-refactor results below remain the latest evidence.

## Module layout and current checkpoint, September 22, 2026

Reviewed `86f9f2e` plus Nick's uncommitted Chapter 1C and guide changes, then the
behavior-preserving organization pass. Updated chapter locations, maintenance
test targets, the browser tick walkthrough, and the explanation of `mod` and
`pub use`. Existing chapter names and anchors remain stable. Applied the locally
installed TutorBro skill to these navigation and explanation changes.

The current implementation now lives in `energy.rs`, `simulation.rs`,
`components.rs`, `config.rs`, `journal.rs`, `fixture.rs` and `lessons.rs`;
`lib.rs` preserves public root imports through re-exports. Existing maintenance
test names are unchanged in the new `tests/maintenance.rs` target. The library
now has two unit tests and five integration tests, still seven in total.

Copied the current manifests, lockfile, scripts and crate sources to
`/private/tmp/moss-module-checkpoint-ob0on2kj`. Inserted the exact
`individual-energy-component` fence from Chapter 1D into that copy's `energy.rs`
and replaced its root export with the exact `individual-energy-export` fence.
Kept `CARGO_TARGET_DIR` at that workspace's own `target` directory.

Ran `scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline` and
`scripts/with-toolchain.sh cargo fmt --all -- --check` there. Observed seven
passing tests, zero failures, and clean formatting. This establishes the
relocated next edit compiles; it does not attach the component or implement its
runtime use. No future biological behavior was installed in the live project.
The shell's non-writable pyenv-shims notice did not prevent either command.

The live workspace passed all 12 native tests, native/WASM Clippy, formatting,
JavaScript syntax, a release bundle build and API docs. The exact updated IDE
maintenance command ran one matching test successfully. The rebuilt browser
passed default-rate Step, inspection, camera, Play/Pause and Reset smoke checks;
see [build evidence](../builds/2026-09-22.md#module-organization--september-22-2026).
Custom-rate browser/reset acceptance, inspector rate display and future
population/foraging behavior remain pending. RustRover rendering was not
rechecked. Earlier Chapter 1A–C and foraging worked-function fences were not
changed or rerun; their historical executable records remain below.

Final static validation passed 270 local file/heading links and fenced-block
closure across 23 guide, entry-point and reference documents. `git diff --check`
also passed.

## Earlier evidence

[September 21 examples and research](2026-09-21.md) record
the original red/green walkthroughs and superseded proposals.

## Reproducible movement answer, September 22, 2026

Extended the existing isolated example runner with `--movement`; the default
sixteen-reference mode and `--session` remain available. The new mode extracts
v2's exact helper, compares both printed answers after pinned rustfmt, validates
the copied function boundary and maintenance-only schedule, and checks the helper
before activating movement in that copy. It requires each expected test's passing
result, so an empty or ignored selection is not sufficient. Clippy follows the
five prepared movement/maintenance tests. No live rule or test is changed.

The activation run uses `--include-ignored` instead of editing test attributes,
following the official [libtest selection documentation](https://doc.rust-lang.org/rustc/tests/index.html#--include-ignored).
The runner is intentionally specific to the current source shape; a changed
signature or schedule requires review. It uses a separate target directory and
the copied toolchain pin. Retaining a copy is optional; ordinary runs remove it
on success or failure. The authoring guide documents this agent-owned command.

Ran from the live repository root:

```sh
python3 -B docs/tutorial/authoring/check_path_examples.py --movement --keep-workspace
```

The retained workspace was
`/private/var/folders/dc/n3zh722n3_s3tfb4806dh9mh0000gn/T/moss-path-examples-z8vipvmo`.
The runner executed, with that copy's own `CARGO_TARGET_DIR`:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test movement movement_charges_only_an_affordable_actual_step -- --exact --format pretty --color never
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test movement --test maintenance -- --include-ignored --format pretty --color never
scripts/with-toolchain.sh cargo clippy -p moss-sim --locked --offline --test movement --test maintenance -- -D warnings
```

Observed one passing helper test with two filtered out, then **five passing tests,
none ignored**, and clean Clippy. Copied test files remain byte-identical to live
tests, including their ignore attributes. No `month_guide` target was created in
movement mode. The retained copy contains the answer and activated schedule; the
live project still contains its unfinished, unscheduled helper.

To check the existing default mode and directory independence, ran from
`/private/tmp`:

```sh
python3 -B /Users/nick/Code/moss/docs/tutorial/authoring/check_path_examples.py
```

It extracted the current sixteen references and passed **16 tests, none ignored**.
Its workspace, `moss-path-examples-c6h3781r`, was removed afterward as requested by
the default cleanup behavior. This fresh full pass includes the later distance
and shared-food cases. Path Clippy and earlier diagnostic targets were not rerun;
their preceding results remain in the dated path record. The shell's pyenv-shims
notice did not prevent execution. `--help` also completed successfully.

These checks verify native examples and the runner, not a live solution, a full
workspace run, WASM, browser movement or native RustRover rendering. V2's prose,
all learner examples and the active task remain unchanged.

A bounded reviewer supplied eleven focused stdlib fixtures. They exposed an
ambiguous duplicated-answer match, which was corrected before the final check.
The fixtures verify missing/duplicate fences and functions, signature/schedule
drift, empty or ignored test selections, failed-stage sequencing, copied writes,
cleanup/retention and conflicting mode flags. The lead inspected and ran them:

```sh
python3 -B -m unittest discover -s docs/tutorial/authoring -p 'test_check_path_examples.py' -v
python3 -B docs/tutorial/authoring/check_path_examples.py --movement
python3 docs/tutorial/authoring/check_docs.py
git diff --check
```

Observed **11 passing runner fixtures**. The final movement invocation again
passed one helper test, all five activated movement/maintenance tests and Clippy;
its `moss-path-examples-mbb6d594` workspace was removed afterward. Navigation
passed 726 local destinations across 75 Markdown files, and whitespace checks
passed. Of 48 files captured for this pass, 47 lesson/source files remain
byte-identical; only the path evidence summary changed to record its fresh run.
The earlier 41-file protection set also matches. No live behavior was activated.

## Copy and borrow walkthrough, September 22, 2026

Added an optional conversation visual for the existing v2 answer. Seven selected
source checkpoints distinguish the caller's Position from the copied `next`,
then show position write-back, energy subtraction and return. Inputs stay fixed:
start (2,2), target (4,4), reserve 59, travel cost 2. Maintenance is outside the
helper. Navigation changes the illustration, not the live world or its history.

A bounded read-only reviewer checked the Rust interpretation. The local remains
available after `*position = next` because Position is Copy. The illustrated
position-then-energy writes are consecutive source statements, not separately
rendered simulation ticks or an atomic transaction. The visual labels selected
statements, valid coordinates and the supplied reserve; it adds no learner task.

The durable conversation source is
`/Users/nick/.codex/visualizations/2026/09/20/01a0c105-6b3b-73b1-8900-b14177405987/movement-copy-and-borrow.html`.
It contains fixed illustrated states, not another implementation of the movement
rule. The existing lesson and project diagrams were not edited or replaced.

In the retained `moss-path-examples-z8vipvmo` workspace, extracted the exact v2
helper and added instrumentation at those statement boundaries in a separate
`movement_copy_trace` target. Its one regression checks all seven literal states,
unchanged y coordinates and the returned distance. The displayed values were
compared with that test's expected sequence. Using the copy's own target directory:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test movement_copy_trace
scripts/with-toolchain.sh rustfmt --edition 2024 crates/moss-sim/tests/movement_copy_trace.rs
scripts/with-toolchain.sh cargo clippy -p moss-sim --locked --offline --test movement_copy_trace -- -D warnings
```

Observed **one test passed** and clean Clippy. Instrumentation observes the
worked answer in isolation; it does not call the unfinished live helper or
establish browser movement. The scratch target is not a checked-in project API.

The visual's temporary Chromium preview was checked at 736px and 320px widths.
All seven stages, backwards navigation, disabled endpoints, keyboard activation
and state restoration after reload were observed. At 320px, the content and
scroll widths both measured 288px inside the preview margin, with no clipped SVG
text. No console warnings or errors were reported. The final labels were checked
after reloading the preview. Temporary viewport overrides were reset, and the
owned preview tab and loopback server were closed.

No live source, current lesson, reference fence or NOW changed. No new WASM
build, Moss browser observation, native RustRover preview or dark-theme inspection
was performed. Host conversation embedding and assistive-technology behavior
remain distinct from the observed standalone Chromium preview.

## Query access checkpoint, September 22, 2026

Added optional context explaining why removing `Without<Creature>` from the
prepared adapter can compile but fail when Bevy initializes the system. A bounded
read-only reviewer checked the teaching distinction against the adapter,
[Bevy ECS 0.18.1 Query documentation](https://docs.rs/bevy_ecs/0.18.1/bevy_ecs/system/struct.Query.html#disjoint-queries)
and [B0001 guidance](https://bevy.org/learn/errors/b0001/). Cached pinned
`system_param.rs` confirms that query initialization checks declared access
compatibility. The current filters already establish disjoint membership.
The final prose review found no further issues.

In the retained workspace
`/private/var/folders/dc/n3zh722n3_s3tfb4806dh9mh0000gn/T/moss-path-examples-1o4zl3h_`,
all eight simulation source files were byte-compared with live source before
adding a separate `query_access` test target. The copied helper remains unfinished
and movement unscheduled. Ran, using that copy's own target directory:

```sh
scripts/with-toolchain.sh rustfmt --edition 2024 crates/moss-sim/tests/query_access.rs
CARGO_TARGET_DIR="$PWD/target" scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test query_access -- --nocapture
CARGO_TARGET_DIR="$PWD/target" scripts/with-toolchain.sh cargo clippy -p moss-sim --locked --offline --test query_access -- -D warnings
```

Observed **four passing tests, none ignored**, and clean Clippy:

- A copied query pair with only the exclusion removed failed initialization
  with the expected B0001 in an empty world.
- The same declarations failed with the existing disjoint fixture and a
  single-threaded schedule. The system body did not run.
- The actual prepared `move_to_food` initialized successfully without executing
  its body or unfinished helper.
- A synthetic entity carrying all six required components was excluded from
  the correctly filtered patch query and included in the creature query.
  This was a membership probe, not a proposed creature or biology change.

Two tests pass by expecting the specific B0001 panic. The diagnostic redacts
component names with Moss's current feature set; the explanation does not invent
a fully named error message. The shell's pyenv-shims notice did not prevent
execution. These checks support initialization and membership claims only;
they do not activate movement or verify browser behavior. Live source, today's
lesson, NOW and the future references remain unchanged.

Navigation validation passed **731 local destinations across 75 Markdown files**;
`git diff --check` passed. All 41 files in the original protection set and all 47
lesson/source files in the later runner set remain byte-identical. The runner
set's path evidence summary retains its previously recorded update. No WASM,
browser, native IDE or full-workspace checks were repeated for this context edit.

## Native reading checkpoint, September 22, 2026

Inspected the lesson in the installed RustRover EAP, bundle
`com.jetbrains.rustrover-EAP`, build `RR-263.5153.48`. The app's local Info.plist
confirmed that version. A bounded read-only route review found two optional
context links calling v1 today's task; redirected them to v2. The IDE setup
guide had the same stale destination and was corrected. Original lesson and
companion-chapter links remain available where explicitly identified as such.

Using native computer-use observations and actions, opened Search Everywhere,
selected `today-v2.md` and used **Open In Right Split**. The lesson preview sat
beside `lessons.rs`, with the Run tool window hidden for reading space. Observed:

- V2's title, prose, contents, code fences and diagnostic table rendered in the
  existing dark IDE theme. Long code lines have horizontal scrollbars.
- The section 2 and section 5 contents links navigated to their named headings.
- Both complete local SVGs, movement-step and adapter-to-helper, rendered in
  the native Markdown preview. Labels and diagram edges were visible in the
  approximately 400-pixel-wide pane in the returned screenshots.
- **Guide home** opened the existing tutorial README tab; its v2 link returned
  to the already open lesson. Restored `lessons.rs` beside v2 afterward.

Some background scroll captures were blank or only partially painted. Switching
to **Editor and Preview**, then back to **Preview**, restored painted content.
Smaller scrolls also exposed the complete diagrams. This records an observed
automation/capture limitation without attributing a root cause or claiming that
ordinary foreground reading has the same defect. The final capture shows v2's
opening beside the Rust source. The new split remains available for Nick.

The preferred cua-driver CLI was unavailable as a working daemon: status said
not running, and its documented serve attempt exited 1. An out-of-daemon
permission probe warned that its result could be inaccurate and stalled; stopped
that owned probe with Ctrl-C, exit 130. No permission or installation change was
made. The available computer-use app API then inspected RustRover successfully.

Updated the reading guide with the observed split-opening route and precise
preview coverage. The [separate native Run check](../builds/2026-09-22.md#movement-action-in-rustrover--september-22-2026)
reached the intentional unfinished-helper failure. This pass does not establish
the sixteen future guides' IDE rendering, other themes, screen-reader behavior,
native Debug, browser activation or a completed learner implementation.

Navigation validation passed **741 local destinations across 75 Markdown files**;
`git diff --check` passed. All 42 protected source, configuration and current-lesson
hashes match after IDE execution. No dependency, live rule or run-configuration
file changed. Earlier records of unverified IDE behavior describe their earlier
checkpoints; current summaries now link to this bounded inspection.


## Meal handoff checkpoint, September 22, 2026

Reviewed the route from v2 into the finite-meal companion. A bounded independent
reader review found that Chapter 5's returns still led to v1 and that its
relationship to short sessions 01–02 was implicit. Updated its current-session
links, mapped checkpoints A/B to those sessions, and routed the following step
to the canonical food-selection lesson. Session 02 now explicitly preserves
an already verified stretch regression and browser arrival. Neither route asks
for a second implementation of completed work. V2 itself was not changed.

The companion's future live test used only bite limit 4, unlike session 01's
stronger reference. Added zero and positive-2 cases to the same existing test,
with literal reserve/biomass assertions. The prose identifies reserve reset as
test setup and explains why the smaller bite distinguishes a wrong rule. Existing
shared-food assertions remain. The helper body is unchanged. Pinned rustfmt
formatted the printed test. The final independent prose review found no issues.

Native checks used the retained isolated workspace
`/private/var/folders/dc/n3zh722n3_s3tfb4806dh9mh0000gn/T/moss-path-examples-1o4zl3h_`,
its own `CARGO_TARGET_DIR`, and the copied pinned toolchain/lockfile. Compared all
copied simulation source files with live source before use; they match. Extracted
the marked Chapter 5 test and helper into one temporary integration target. The
only extraction adaptation changed the test's future helper import to use the
local printed helper; `Energy` and `FoodPatch` still came from `moss_sim`.
This verifies the written rule/test, not a helper installed in `lessons.rs`.

The controlled faulty helper replaced the configured positive bite limit with
4, while retaining explicit zero. Ran in that workspace:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test chapter5_before_fixed_bite --test chapter5_meal_reference
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test chapter5_after_fixed_bite
scripts/with-toolchain.sh cargo clippy -p moss-sim --locked --offline --test chapter5_meal_reference -- -D warnings
```

The faulty helper passed the old test. The unchanged correct helper passed the
revised test. The faulty helper then failed the revised test, returning 3 where
the smaller configured bite required 2; that process exited 101. Clippy passed
for the correct reference. Both mutant targets were removed after verification;
the correct isolated reference remains available. No full path rerun, movement
activation, installed meal adapter, WASM build or browser/IDE check ran here.
These results concern a test discriminator and route consistency, not measured
learning effectiveness or new live behavior.

Navigation validation passed **748 local destinations across 75 Markdown files**;
`git diff --check` passed. Of 44 files snapshotted for this review, only Chapter 5
and session 02 changed. Live source, NOW, v2, the original movement lesson and all
sixteen runnable reference fences remain unchanged.


## Current-route navigation, September 22, 2026

After checkpoint `83a72a5` was pushed to origin, audited current-entry links from
setup, ecology, completed maintenance, individual costs and the movement
companion. A bounded independent review identified ten links labeled current or
today that still led to v1. Retargeted them to v2. The conceptual map and test
context now lead directly to the relevant v2 sections; future food selection and
seeking links lead to their canonical short sessions.

Chapter 4 now explicitly identifies itself as the companion for the same edit.
The original lesson gained only an original-version banner linking v2; its
previous body and internal links are byte-identical. Updated the movement stub's
one guide-reference comment to v2. Its signature, placeholder and all executable
source remain unchanged. No test, schedule, printed Rust example or biology changed.

Compared all changed Markdown Rust fences with the checkpoint: they match.
Of 23 protected source/current-state files, only the documented comment and
original-version banner differ. NOW and v2 are unchanged. Navigation validation
passed **752 local destinations across 75 Markdown files**; pinned rustfmt and
`git diff --check` passed. No native test, WASM, browser or IDE run was repeated
for this navigation-only change. Existing evidence keeps its original scope.
