# Build checks — September 22, 2026

## Module organization — September 22, 2026

Refactored `86f9f2e` plus Nick's existing uncommitted Chapter 1C work. Kept both
Cargo packages, dependency versions, lockfile, public `moss_sim` paths, and all
biological behavior. The library root is now 21 lines of module declarations
and public exports. The browser host shrank from 403 to 63 lines, with bridge,
frame, scene and snapshot code in child modules. Every Rust file, including
integration tests, is now at most 125 lines. See [the code map](../../../README.md#code-map).

Moved maintenance regressions into `tests/maintenance.rs`, configuration checks
beside `WorldConfig`, and the eviction test beside `Journal`. Updated the shared
RustRover maintenance target and tutorial paths. A read-only helper compared
against a saved copy of the actual starting worktree: all 32 simulation/test
function signatures and bodies matched after excluding comments and whitespace;
all assertions were retained, and `lessons.rs` was byte-identical. No later
learner exercise was implemented.

Actual checks, through the existing pinned toolchain:

- `scripts/check.sh`: passed before and after the refactor; all 12 native tests
  (7 simulation, 5 presentation), formatting, native/WASM Clippy with warnings
  denied, and JavaScript syntax passed.
- `scripts/with-toolchain.sh trunk build --locked --release`: passed.
- `scripts/with-toolchain.sh cargo doc -p moss-sim --no-deps --locked`: passed.
- `scripts/with-toolchain.sh cargo test -p moss-sim --locked --test maintenance -- maintenance_spends_energy_and_stops_at_zero --exact`:
  one test passed, one filtered out. This verifies the updated IDE command;
  the RustRover UI itself was not exercised again.
- The exact Chapter 1D component and new re-export examples compiled in an
  isolated workspace, with seven simulation tests and formatting passing.
  [Guide evidence](https://github.com/NicholasHazen/moss/blob/015ecdbf76a4c5253df8e1b182c0bee84c0cca9e/docs/tutorial/authoring/verification.md#module-layout-and-current-checkpoint-september-22-2026)
  records its separate target directory and limits.

Served the rebuilt `dist` bundle with
`python3 -m http.server 8081 --bind 127.0.0.1 --directory dist`. The initial
sandboxed listener was denied; the approved loopback-only invocation succeeded.
In-app Chromium rendered the grid, three markers and selection outline. Three
Steps left Hare and Fox at 57 / 100 on tick 3. Zoom changed 19.2 to 24.0 px/cell;
a 60px/30px drag moved the scene while preserving selection, tick and energy.
Clicking the moved hare marker selected Hare #1 at its unchanged cell (10, 10).
Fit world restored 19.2 px/cell. Play advanced the simulation; Pause held tick
187 with Hare at zero and all three entities still present. Grass still showed
80 biomass at (16, 13). Reset produced run 2 at tick 0, cleared selection,
restored Hare to 60 / 100 and replaced the journal with four startup events.
No warning or error entries were returned by the browser log check.

This verifies the default-rate browser behavior after reorganization. Custom-rate
reset/browser acceptance and rate display remain future Chapter 1 work. Resize,
real-browser hidden-tab behavior, other browsers, and performance were not
retested. No public deployment, tool installation, or dependency update occurred.

Checked official [Rust module file conventions](https://doc.rust-lang.org/book/ch07-05-separating-modules-into-different-files.html),
[visibility and re-exports](https://doc.rust-lang.org/reference/visibility-and-privacy.html),
and [Cargo package layout](https://doc.rust-lang.org/cargo/guide/project-layout.html)
before choosing the module layout. Compile and runtime evidence above establish
this particular refactor's compatibility.


## Movement session preparation — September 22, 2026

Prepared an authored Fern → Meadow target, shared travel rate 2, an unscheduled
ECS adapter, a learner-owned `move_one_cell` stub, tests, inspector facts and a
shared RustRover run configuration. No dependencies changed. Full live checks
are intentionally red until the learner implements the helper.

Commands actually run from the live root:

```sh
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline
scripts/with-toolchain.sh cargo test -p moss-web --locked --offline
scripts/with-toolchain.sh cargo test -p moss-sim --locked --offline --test movement movement_charges_only_an_affordable_actual_step -- --exact
scripts/with-toolchain.sh cargo fmt --all -- --check
scripts/with-toolchain.sh cargo clippy --workspace --all-targets --locked --offline -- -D warnings
scripts/with-toolchain.sh cargo clippy -p moss-web --target wasm32-unknown-unknown --locked --offline -- -D warnings
node --check crates/moss-web/shell.js
scripts/with-toolchain.sh trunk build --locked --release
```

Observed: seven existing simulation tests pass; movement has one expected
`todo!()` failure and two explicitly ignored acceptance tests. The exact focused
command runs one failing test with two filtered out. All five presentation tests,
formatting, native/WASM Clippy, JS syntax and the release build pass. `scripts/check.sh`
would stop at the intentional exercise failure; it is not claimed green.

An isolated workspace `/private/tmp/moss-today-fdz47wii` used its own target
folder. The exact Chapter 4 answer first passed 13 workspace tests with two
ignored; after activating movement and both acceptance tests, all 15 passed.
See [guide evidence](https://github.com/NicholasHazen/moss/blob/015ecdbf76a4c5253df8e1b182c0bee84c0cca9e/docs/tutorial/authoring/verification.md) for the distinction
between a worked answer and live implementation.

Served the built `dist` on loopback 8080 and inspected it in in-app Chromium:
startup, Fern rate 1 and authored target #3, three Steps yielding Fern and Flint
57 with unchanged positions, and Reset yielding Fern 60 at (10,10), target #3,
tick 0 in a new run. Movement was unscheduled throughout this live check. No
travel or meal browser result is claimed. Both tutorial SVGs were served locally
and checked visually for readable labels and unclipped layout.

The sandbox blocked the initial loopback bind; the authorized retry with elevated
execution succeeded. No external hosting or installation occurred. Native IDE
Debug, the new IDE Run action, IDE Markdown preview, custom-rate browser acceptance,
and broader camera/hidden-tab behavior were not rechecked in this pass.

After smoke checks, stopped both temporary static servers and started the normal
`scripts/with-toolchain.sh trunk serve --locked --release` preview on loopback
8080. The agent-owned terminal session is 18808; it can be stopped with Ctrl-C
before starting an IDE preview. No second preview should bind the same port.

## Movement action in RustRover — September 22, 2026

Selected the existing **Moss - Movement test** entry under **All Configurations**
and clicked its normal Run button in RustRover EAP `RR-263.5153.48`. The test tree
reported one failed test, named
`movement_charges_only_an_affordable_actual_step`. The console reported the
intended `not yet implemented: Paired movement exercise: propose, validate, then
commit one step` panic at `lessons.rs:67:5`, reached from `tests/movement.rs:20:9`.
The process finished with **exit code 101**. The source still contains `todo!()`.

The generated IDE invocation shown in its console was:

```text
/Users/nick/.cargo/bin/cargo test --color=always -p moss-sim --locked --test movement movement_charges_only_an_affordable_actual_step --profile test --no-fail-fast --config target.aarch64-apple-darwin.runner=['/Users/nick/Applications/RustRover.app/Contents/plugins/intellij-rust/bin/mac/aarch64/intellij-rust-native-helper'] -- --format=json --exact -Z unstable-options --show-output
```

This is an observed IDE invocation, not a replacement for the normal terminal
command. It confirms configuration discovery, native test execution and arrival
at the learner's unfinished body. It does not establish a passing helper,
movement activation or debugger health. No run-configuration file or source was
edited. Left the movement configuration selected, retained its result in the
Run tool window and hid that window to restore the reading layout.

The [native reading record](../tutorial/2026-09-22.md#native-reading-checkpoint-september-22-2026)
covers v2's preview and its background repaint limitation. No new WASM build,
full-workspace suite, browser run or Debug session was performed in this pass.

## Native debugger retry — September 22, 2026

Retried **Moss - Maintenance test** in RustRover EAP `RR-263.5153.48` with the
temporary logger `#com.jetbrains.cidr.execution.debugger:trace`. Its original
logging field was empty. The first attempt failed before LLDB startup: Cargo's
native linker reported `missing DEVELOPER_DIR path:
/Library/Developer/CommandLineTools`. The project environment was being inherited;
its selected directory no longer existed. This is evidence for this build failure,
not a diagnosis of the earlier September 21 debugger stall.

Read-only compiler checks returned:

```text
xcode-select -p
/Applications/Xcode.app/Contents/Developer

xcrun --sdk macosx --show-sdk-path
/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX27.0.sdk

xcrun --find clang
/Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/clang

xcrun clang --version
Apple clang version 21.0.0 (clang-2100.3.34.2)
Target: arm64-apple-darwin25.6.0
```

Removed only the obsolete environment entry through **Settings → Rust →
Environment variables**. Verified the saved `RustProjectSettings` in ignored
`.idea/workspace.xml` retained the Cargo toolchain path and no longer contained
the override. The existing shell wrapper already leaves compiler selection alone
when the standalone tools directory is absent; it needed no change.

Clicked Debug again. The IDE freshly compiled `moss-sim`; the log recorded bundled
`LLDBFrontend` startup at 20:25:30. The Debug test tree reported **1 test passed**,
and its console showed this invocation followed by **exit code 0**:

```text
/Users/nick/Code/moss/target/debug/deps/maintenance-c1510f3f275c6f8a maintenance_spends_energy_and_stops_at_zero --format=json --exact -Z unstable-options --show-output
```

Cleared the temporary logger back to its original empty field, restored
**Moss - Movement test** as the selected configuration and hid the Debug tool
window to return to the open v2 lesson. No source, shared run configuration,
dependency, system-wide compiler selection or security setting changed. No
restart, installation or log upload occurred. A bounded read-only review found
the shared maintenance Cargo configuration consistent; no configuration rewrite
was warranted.

This establishes one native test's Debug execution. Breakpoints, stepping,
variable inspection, movement Debug, a full native suite, a new WASM build and
browser smoke checks were not exercised in this pass. The earlier stall did not
recur, but its historical cause is not established. The learner-owned movement
helper remains unfinished and unscheduled.

Documentation validation checked 760 local destinations across 75 Markdown
files; `git diff --check` passed. A before/after SHA-256 comparison preserved all
28 audited source, shared run-configuration and v2 lesson files. `NOW.md` changed
only its debugger-coverage wording. These checks do not extend runtime coverage.
